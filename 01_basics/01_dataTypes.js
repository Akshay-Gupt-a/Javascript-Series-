"use strict" // treat all JS codes as newer version
// alert(3+3) // we are using nodejs , not browser
//console.log(3+3); console.log("Akshay");// Not good habbit , code should be readable

// number => 2 to power 53
let age=18
// string '' "" ``
// boolean => true/false
// null => standalone value // Consider you are fetching weather but it's unable to send then it's send null (not 0 will send not undefined it will send null means nothing )
// undefined => not give value;
// symbol =>unique

//object

console.log(typeof("2"))//string
console.log(typeof null)//object (language error)
console.log(typeof undefined)// undefined